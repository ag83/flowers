{
  "name": "flowers",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "kill-ports": "npx kill-port --port 3001,3000",
    "start": "npm run kill-ports && lerna run start --parallel",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
